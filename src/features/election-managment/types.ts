export type ERORRS = {
  name: string;
  proposal: string;
};

export type INITIALSTATE_ELECTION_FORM = {
  success: boolean;
  message: string;
};

export type ELECTION = {
  name: string;
  proposal: string;
};
